bot builder UI -> bot builder -> traning gateway -> gọi api GCP -> training 
server có sẵn dcoker, dữ liệu server tự lấy trong DB

- vẽ lại state diagram, khớp chuyển trạng thái của node, ( (để sau) nghiên cứu thêm spot instance + interuption notice)
- tạo bot nhỏ để train trên dev
- học SDK K8s (kubeconfig)
- API K8s (để sau ) 

Require
- tạo deployment bằng code, dùng SDK
- quản lý training deployment bằng code 
- giao diện quản lý training: server nào đang chạy(bao tiếng, bao tiền, nếu thời gian chạy quá lâu, phải có alert cảnh báo), bot nào đang training, health check probe
- chuyển sang AWS, đo đạc với GCP
- estimate dữ liệu nào với server nào


Mục tiêu chính 
GCP -> AWS (-> spot instance) VD: cùng dữ liệu, GCP mất ? AWS mất ?, visualize
đảm bảo thời gian huấn luyện 
tiết kiệm tiền



Output kì này
chuyển sang AWS, đo đạc với GCP
estimate dữ liệu nào với server nào


Status Server : khởi động chưa, đang chạy bị tắt...
Vừa train xong, server chưa kịp tắt, thì có thằng khác gọi tới (=> chưa chết), nhưng thực ra đang sắp chết, xử lý thế nào ?
Retry K8s hot instance, đang chạy dở sập luôn, thì phải làm sao để đỡ tốn công, => log lại weight 


điều phối server: tìm server phù hợp nhất với tác vụ cần train


cái cũ: bật lên train luôn
cách mới: tạo node pod mất thời gian, nếu task train 1 phút mà bật mất 5p thì k hợp lý
